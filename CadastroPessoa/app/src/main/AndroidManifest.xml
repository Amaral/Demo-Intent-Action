<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fiap.mob.cadastropessoa" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroPessoaFisicaActivity"
            android:label="@string/title_activity_cadastro_pessoa_fisica" >
        </activity>
        <activity
            android:name=".CadastroPessoaJuridicaActivity"
            android:label="@string/title_activity_cadastro_pessoa_juridica" >
        </activity>
    </application>

</manifest>
